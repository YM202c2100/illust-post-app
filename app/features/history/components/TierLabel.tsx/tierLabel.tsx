export const TierLabel:React.FC = ()=>{
  const viewHeight = 600
  return(
    <svg xmlns="http://www.w3.org/2000/svg" 
      viewBox={`0 0 ${60} ${viewHeight}`} 
      width={60} height={viewHeight} 
    >

    </svg>
  )
}